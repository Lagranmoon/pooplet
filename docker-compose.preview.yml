services:
  postgres:
    image: postgres:18-alpine
    container_name: pooplet-preview-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-pooplet_preview}
      POSTGRES_USER: ${DB_USER:-pooplet}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - ./data/preview-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-pooplet} -d ${DB_NAME:-pooplet_preview}"]
      interval: 30s
      timeout: 10s
      retries: 3

  app:
    build: .
    container_name: pooplet-preview-app
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://${DB_USER:-pooplet}:${DB_PASSWORD}@postgres:5432/${DB_NAME:-pooplet_preview}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "3001:3000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
